.idea
dist
node_modules
*.DS_Store
coverage
npm-debug.log
.vscode
/venv
.vs/

# Wrangler / Cloudflare Pages local secrets (e.g. TURNSTILE_SECRET_KEY)
.dev.vars
.wrangler/

# Playwright (tests/visual/*-snapshots/ baselines are committed, not ignored)
test-results
playwright-report
blob-report
playwright/.cache